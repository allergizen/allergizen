import { View, Text } from 'react-native';
import React from 'react';

const Favorites = () => {
   return (
      <View>
         <Text>Profile</Text>
      </View>
   );
};

export default Favorites;
